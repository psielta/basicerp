@{
    ViewBag.Title = "Home Page";
}

<main>
    <section class="row mb-4" aria-labelledby="aspnetTitle">
        <h1 id="title">Basic - Sistema de Gestão</h1>
        <p class="lead">Sistema multi-tenant com Entity Framework Core e PostgreSQL</p>
    </section>

    <div class="row mb-4">
        <div class="col-md-12">
            <div class="alert alert-success" role="alert">
                <h4 class="alert-heading">Entity Framework Core funcionando!</h4>
                <hr>
                <div class="row text-center">
                    <div class="col-md-4">
                        <h2 class="display-4">@ViewBag.TotalTenants</h2>
                        <p class="text-muted">Tenants cadastrados</p>
                    </div>
                    <div class="col-md-4">
                        <h2 class="display-4">@ViewBag.TotalUsuarios</h2>
                        <p class="text-muted">Usuários cadastrados</p>
                    </div>
                    <div class="col-md-4">
                        <h2 class="display-4">@ViewBag.TotalClientes</h2>
                        <p class="text-muted">Clientes cadastrados</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <section class="col-md-4" aria-labelledby="dbTitle">
            <h2 id="dbTitle">Banco de Dados</h2>
            <p>PostgreSQL rodando no Docker na porta 5432</p>
            <ul>
                <li><strong>Database:</strong> basic_db</li>
                <li><strong>Usuário:</strong> adm</li>
                <li><strong>Tabelas:</strong> tenants, usuarios, clientes</li>
            </ul>
        </section>
        <section class="col-md-4" aria-labelledby="efTitle">
            <h2 id="efTitle">Entity Framework</h2>
            <p>EF Core 3.1.32 configurado com Npgsql provider</p>
            <ul>
                <li>Migrations aplicadas com sucesso</li>
                <li>Injeção de dependência configurada</li>
                <li>DbContext disponível nos controllers</li>
            </ul>
        </section>
        <section class="col-md-4" aria-labelledby="modelsTitle">
            <h2 id="modelsTitle">Modelos</h2>
            <p>Estrutura multi-tenant implementada</p>
            <ul>
                <li><strong>Tenant:</strong> Empresas/Organizações</li>
                <li><strong>Usuario:</strong> Usuários do sistema</li>
                <li><strong>Cliente:</strong> Clientes dos tenants</li>
            </ul>
        </section>
    </div>
</main>