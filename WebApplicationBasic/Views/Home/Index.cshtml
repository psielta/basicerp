@{
    ViewBag.Title = "Home Page";
}

<main>
    <section class="row mb-4" aria-labelledby="aspnetTitle">
        <h1 id="title">Basic - Sistema de Gestao</h1>
        <p class="lead">Sistema multi-tenant com Entity Framework Core e PostgreSQL</p>
    </section>

    <div class="row mb-4">
        <div class="col-md-12">
            <div class="alert alert-success" role="alert">
                <h4 class="alert-heading">Entity Framework Core funcionando!</h4>
                <hr>
                <div class="row text-center">
                    <div class="col-md-4">
                        <h2 class="display-4">@ViewBag.TotalOrganizations</h2>
                        <p class="text-muted">Organizations (tenants)</p>
                    </div>
                    <div class="col-md-4">
                        <h2 class="display-4">@ViewBag.TotalUsers</h2>
                        <p class="text-muted">Usuarios globais</p>
                    </div>
                    <div class="col-md-4">
                        <h2 class="display-4">@ViewBag.TotalMemberships</h2>
                        <p class="text-muted">Memberships ativos</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <section class="col-md-4" aria-labelledby="dbTitle">
            <h2 id="dbTitle">Banco de Dados</h2>
            <p>PostgreSQL rodando no Docker na porta 5432</p>
            <ul>
                <li><strong>Database:</strong> basic_db</li>
                <li><strong>Usuario:</strong> adm</li>
                <li><strong>Tabelas:</strong> organization, "user", memberships, account, session</li>
            </ul>
        </section>
        <section class="col-md-4" aria-labelledby="efTitle">
            <h2 id="efTitle">Entity Framework</h2>
            <p>EF Core 3.1.32 configurado com Npgsql provider</p>
            <ul>
                <li>Migrations prontas para criar extensoes uuid-ossp e citext</li>
                <li>Injecao de dependencia configurada</li>
                <li>DbContext disponivel nos controllers</li>
            </ul>
        </section>
        <section class="col-md-4" aria-labelledby="modelsTitle">
            <h2 id="modelsTitle">Modelos</h2>
            <p>Estrutura multi-tenant pronta para SaaS</p>
            <ul>
                <li><strong>Organization:</strong> representa o tenant</li>
                <li><strong>User:</strong> usuario global com role</li>
                <li><strong>Membership:</strong> vinculo usuario x organization</li>
                <li><strong>Account:</strong> credenciais/OAuth</li>
                <li><strong>Session:</strong> sessoes com IP e user agent</li>
            </ul>
        </section>
    </div>
</main>
