@model WebApplicationBasic.Models.ViewModels.AccountOrganizationsViewModel

@{
    ViewBag.Title = "Minhas Organizações";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid px-4">
    <h1 class="mt-4">Minhas Organizações</h1>
    <ol class="breadcrumb mb-4">
        <li class="breadcrumb-item"><a href="@Url.Action("Index", "Home")">Home</a></li>
        <li class="breadcrumb-item"><a href="@Url.Action("Profile", "Account")">Meu Perfil</a></li>
        <li class="breadcrumb-item active">Organizações</li>
    </ol>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="bi bi-check-circle"></i> @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <div class="row">
        @if (Model.Organizations != null && Model.Organizations.Any())
        {
            foreach (var org in Model.Organizations)
            {
                <div class="col-xl-4 col-lg-6 mb-4">
                    <div class="card h-100 @(org.IsCurrent ? "border-primary border-2" : "")">
                        @if (org.IsCurrent)
                        {
                            <div class="card-header bg-primary text-white">
                                <small><i class="bi bi-check-circle"></i> Organização Atual</small>
                            </div>
                        }
                        <div class="card-body">
                            <div class="d-flex align-items-center mb-3">
                                @if (!string.IsNullOrEmpty(org.Logo))
                                {
                                    <img src="@org.Logo" alt="@org.Name" class="rounded me-3" style="width: 60px; height: 60px; object-fit: cover;">
                                }
                                else
                                {
                                    <div class="rounded bg-secondary d-inline-flex align-items-center justify-content-center me-3" style="width: 60px; height: 60px;">
                                        <i class="bi bi-building text-white" style="font-size: 24px;"></i>
                                    </div>
                                }
                                <div class="flex-grow-1">
                                    <h5 class="card-title mb-1">@org.Name</h5>
                                    <span class="badge bg-info">@org.Role</span>
                                </div>
                            </div>

                            @if (!string.IsNullOrEmpty(org.Description))
                            {
                                <p class="card-text text-muted small">@org.Description</p>
                            }

                            <div class="small text-muted">
                                <div class="mb-1">
                                    <i class="bi bi-link-45deg"></i> <strong>Slug:</strong> @org.Slug
                                </div>
                                <div class="mb-1">
                                    <i class="bi bi-people"></i> <strong>Membros:</strong> @org.MemberCount
                                </div>
                                <div>
                                    <i class="bi bi-calendar"></i> <strong>Membro desde:</strong> @org.JoinedAt.ToString("dd/MM/yyyy")
                                </div>
                            </div>
                        </div>
                        <div class="card-footer bg-transparent">
                            @if (!org.IsCurrent)
                            {
                                <a href="@Url.Action("SwitchOrganization", "Auth", new { organizationId = org.OrganizationId })" class="btn btn-sm btn-primary">
                                    <i class="bi bi-arrow-right-circle"></i> Acessar
                                </a>
                            }
                            else
                            {
                                <button class="btn btn-sm btn-success" disabled>
                                    <i class="bi bi-check-circle"></i> Ativa
                                </button>
                            }

                            @if (org.Role == "Owner" || org.Role == "Admin")
                            {
                                <button class="btn btn-sm btn-outline-secondary" title="Gerenciar" disabled>
                                    <i class="bi bi-gear"></i> Gerenciar
                                </button>
                            }
                        </div>
                    </div>
                </div>
            }
        }
        else
        {
            <div class="col-12">
                <div class="alert alert-info">
                    <i class="bi bi-info-circle"></i> Você ainda não faz parte de nenhuma organização.
                </div>
            </div>
        }
    </div>

    <div class="card mt-4">
        <div class="card-header">
            <h5 class="card-title mb-0">Informações sobre Organizações</h5>
        </div>
        <div class="card-body">
            <h6>Funções nas Organizações:</h6>
            <ul>
                <li><strong>Owner (Proprietário):</strong> Controle total sobre a organização, pode excluir a organização.</li>
                <li><strong>Admin (Administrador):</strong> Pode gerenciar membros, configurações e recursos.</li>
                <li><strong>Member (Membro):</strong> Acesso padrão aos recursos da organização.</li>
                <li><strong>Viewer (Visualizador):</strong> Acesso somente leitura aos recursos.</li>
            </ul>

            <div class="alert alert-warning mt-3">
                <i class="bi bi-exclamation-triangle"></i> Para trocar de organização, clique em "Acessar" na organização desejada ou use o menu suspenso no canto superior direito.
            </div>
        </div>
    </div>
</div>